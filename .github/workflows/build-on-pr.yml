name: Run Gradle on PRs
on: [pull_request, workflow_dispatch]
jobs:
  gradle:
    runs-on: ubuntu-latest
    steps:
    - uses: sparkfabrik/android-build-action@v1.0.0
      with:
        project-path: "."
        output-path: MessengerPro.apk
        browserstack-upload: true